# 小红书应用风格UI组件设计规范

## 📋 项目概述

**项目名称**：2026年2月广西行 - 小红书风格UI改造  
**设计目标**：将现有页面UI组件改为小红书应用风格  
**核心原则**：**绝对不能变更任何内容，只修改样式和布局**

---

## 🎨 1. 色彩系统

### 1.1 小红书红色系列

| 色彩名称 | HEX值 | RGB值 | 用途 | 对比度 |
|---------|-------|-------|------|--------|
| **XHS Red (主色)** | `#FF2442` | 255, 36, 66 | 主按钮、标签、强调元素 | 白色文字: 4.5:1 ✅ |
| **XHS Red Dark (深色)** | `#E61E3A` | 230, 30, 58 | 主按钮悬停状态 | 白色文字: 4.8:1 ✅ |
| **XHS Red Light (浅色)** | `#FFF0F3` | 255, 240, 243 | 次按钮悬停背景 | 红色文字: 4.2:1 ✅ |
| **XHS Red Faint (淡色)** | `#FFE5EB` | 255, 229, 235 | 装饰背景 | 红色文字: 3.8:1 ⚠️ |

### 1.2 中性色系

| 色彩名称 | HEX值 | 用途 |
|---------|-------|------|
| **纯白** | `#FFFFFF` | 次按钮背景、卡片背景 |
| **近白** | `#FAFAFA` | 页面背景 |
| **深灰** | `#333333` | 主要文字 |
| **中灰** | `#666666` | 次要文字 |
| **浅灰** | `#999999` | 辅助文字 |

### 1.3 渐变方案

```css
/* 小红书品牌渐变 */
xhs-gradient: linear-gradient(135deg, #FF2442 0%, #FF6B9D 100%)

/* 按钮悬停渐变 */
xhs-gradient-hover: linear-gradient(135deg, #E61E3A 0%, #FF5A8A 100%)

/* 标签渐变 */
xhs-tag-gradient: linear-gradient(90deg, #FF2442 0%, #FF6B9D 100%)
```

### 1.4 阴影系统

```css
/* 小红书风格阴影 */
xhs-shadow-sm: 0 2px 8px rgba(255, 36, 66, 0.15)
xhs-shadow-md: 0 4px 16px rgba(255, 36, 66, 0.25)
xhs-shadow-lg: 0 8px 24px rgba(255, 36, 66, 0.3)
xhs-shadow-hover: 0 12px 32px rgba(255, 36, 66, 0.4)
```

---

## 🔘 2. 按钮规范

### 2.1 主按钮（Primary Button）

#### 样式定义
```css
.btn-primary {
    /* 背景与边框 */
    background: #FF2442;
    border: none;
    border-radius: 8px;  /* 或 4px，根据设计选择 */
    
    /* 文字样式 */
    color: #FFFFFF;
    font-size: 16px;
    font-weight: 600;
    text-align: center;
    
    /* 尺寸 */
    padding: 12px 24px;
    min-height: 44px;  /* 符合触摸目标最小尺寸 */
    min-width: 120px;
    
    /* 阴影 */
    box-shadow: 0 2px 8px rgba(255, 36, 66, 0.3);
    
    /* 交互 */
    cursor: pointer;
    transition: all 0.2s ease;
}

/* 悬停状态 */
.btn-primary:hover {
    background: #E61E3A;
    box-shadow: 0 4px 12px rgba(255, 36, 66, 0.4);
    transform: translateY(-1px);
}

/* 点击状态 */
.btn-primary:active {
    background: #D01A33;
    box-shadow: 0 2px 6px rgba(255, 36, 66, 0.3);
    transform: translateY(0);
}

/* 禁用状态 */
.btn-primary:disabled {
    background: #FFB6C1;
    color: #FFFFFF;
    cursor: not-allowed;
    box-shadow: none;
    transform: none;
}
```

#### 尺寸规范

| 尺寸类型 | 高度 | 内边距 | 字体大小 | 圆角 | 用途 |
|---------|------|--------|---------|------|------|
| **大按钮** | 52px | 16px 32px | 18px | 8px | Hero区域CTA |
| **中按钮** | 44px | 12px 24px | 16px | 8px | 标准按钮 |
| **小按钮** | 36px | 8px 16px | 14px | 6px | 次要操作 |

#### 使用场景
- Hero区域"开始探索"按钮
- 行程卡片主要操作按钮
- 景点/美食卡片主要操作

### 2.2 次按钮（Secondary Button）

#### 样式定义
```css
.btn-secondary {
    /* 背景与边框 */
    background: #FFFFFF;
    border: 1px solid #FF2442;
    border-radius: 8px;
    
    /* 文字样式 */
    color: #FF2442;
    font-size: 14px;
    font-weight: 600;
    text-align: center;
    
    /* 尺寸 */
    padding: 8px 20px;
    min-height: 36px;
    min-width: 80px;
    
    /* 阴影 */
    box-shadow: 0 2px 6px rgba(0, 0, 0, 0.08);
    
    /* 交互 */
    cursor: pointer;
    transition: all 0.2s ease;
}

/* 悬停状态 */
.btn-secondary:hover {
    background: #FFF0F3;
    border-color: #FF2442;
    box-shadow: 0 4px 12px rgba(255, 36, 66, 0.2);
}

/* 点击状态 */
.btn-secondary:active {
    background: #FFE5EB;
    transform: translateY(0);
}

/* 激活状态（用于导航） */
.btn-secondary.active {
    background: #FF2442;
    color: #FFFFFF;
    border-color: #FF2442;
}

/* 禁用状态 */
.btn-secondary:disabled {
    background: #F5F5F5;
    border-color: #E0E0E0;
    color: #999999;
    cursor: not-allowed;
    box-shadow: none;
}
```

#### 尺寸规范

| 尺寸类型 | 高度 | 内边距 | 字体大小 | 圆角 | 用途 |
|---------|------|--------|---------|------|------|
| **大按钮** | 48px | 14px 28px | 16px | 8px | 重要次按钮 |
| **中按钮** | 36px | 8px 20px | 14px | 8px | 标准次按钮 |
| **小按钮** | 32px | 6px 16px | 12px | 6px | 小型次按钮 |

#### 使用场景
- Day标签（Day 1, Day 2, Day 3, Day 4, Day 5）
- Hero区域"2026年2月"标签
- 导航链接激活状态
- 景点类型标签（空心版本）

### 2.3 间距规范

| 元素 | 间距值 | 用途 |
|------|--------|------|
| **按钮组间距** | 12px | 多个按钮水平排列时 |
| **按钮与文字** | 16px | 按钮与周围文字 |
| **按钮组边距** | 24px | 按钮组与容器边缘 |
| **表单按钮** | 20px | 表单底部按钮与内容 |

---

## 🏷️ 3. 标签规范

### 3.1 实心标签（Solid Tag）

#### 样式定义
```css
.tag-solid {
    /* 背景与边框 */
    background: #FF2442;
    border: none;
    border-radius: 6px;
    
    /* 文字样式 */
    color: #FFFFFF;
    font-size: 12px;
    font-weight: 600;
    text-align: center;
    
    /* 尺寸 */
    padding: 4px 12px;
    min-height: 24px;
    display: inline-flex;
    align-items: center;
    gap: 4px;
    
    /* 阴影 */
    box-shadow: 0 2px 4px rgba(255, 36, 66, 0.2);
    
    /* 交互 */
    cursor: default;
    transition: all 0.2s ease;
}

/* 可点击版本 */
.tag-solid.clickable {
    cursor: pointer;
}

.tag-solid.clickable:hover {
    background: #E61E3A;
    transform: translateY(-1px);
    box-shadow: 0 4px 8px rgba(255, 36, 66, 0.3);
}

/* 图标样式 */
.tag-solid i {
    font-size: 10px;
    opacity: 0.9;
}
```

#### 尺寸规范

| 尺寸类型 | 高度 | 内边距 | 字体大小 | 圆角 | 用途 |
|---------|------|--------|---------|------|------|
| **大标签** | 28px | 6px 14px | 14px | 8px | 主要分类标签 |
| **中标签** | 24px | 4px 12px | 12px | 6px | 标准标签 |
| **小标签** | 20px | 3px 10px | 10px | 4px | 小型标签 |

#### 使用场景
- 美食标签："必尝"、"特色"、"推荐"
- 景点标签："历史文化"、"自然风光"、"海滩度假"
- 活动标签："水上活动"、"摄影打卡"

### 3.2 空心标签（Outline Tag）

#### 样式定义
```css
.tag-outline {
    /* 背景与边框 */
    background: #FFFFFF;
    border: 1px solid #FF2442;
    border-radius: 6px;
    
    /* 文字样式 */
    color: #FF2442;
    font-size: 12px;
    font-weight: 600;
    text-align: center;
    
    /* 尺寸 */
    padding: 4px 12px;
    min-height: 24px;
    display: inline-flex;
    align-items: center;
    gap: 4px;
    
    /* 交互 */
    cursor: default;
    transition: all 0.2s ease;
}

/* 可点击版本 */
.tag-outline.clickable {
    cursor: pointer;
}

.tag-outline.clickable:hover {
    background: #FFF0F3;
    border-color: #FF2442;
}

/* 选中状态 */
.tag-outline.selected {
    background: #FF2442;
    color: #FFFFFF;
    border-color: #FF2442;
}

/* 图标样式 */
.tag-outline i {
    font-size: 10px;
    opacity: 0.9;
}
```

#### 尺寸规范

| 尺寸类型 | 高度 | 内边距 | 字体大小 | 圆角 | 用途 |
|---------|------|--------|---------|------|------|
| **大标签** | 28px | 6px 14px | 14px | 8px | 主要分类标签 |
| **中标签** | 24px | 4px 12px | 12px | 6px | 标准标签 |
| **小标签** | 20px | 3px 10px | 10px | 4px | 小型标签 |

#### 使用场景
- 景点类型标签（可选状态）
- 美食分类标签
- 筛选标签

### 3.3 Day标签（Day Badge）

#### 样式定义
```css
.day-badge {
    /* 背景与边框 */
    background: #FFFFFF;
    border: 1px solid #FF2442;
    border-radius: 4px;
    
    /* 文字样式 */
    color: #FF2442;
    font-size: 14px;
    font-weight: 700;
    text-align: center;
    
    /* 尺寸 */
    padding: 6px 16px;
    min-height: 32px;
    display: inline-block;
    
    /* 阴影 */
    box-shadow: 0 2px 6px rgba(0, 0, 0, 0.08);
    
    /* 交互 */
    transition: all 0.2s ease;
}

/* 悬停状态 */
.day-badge:hover {
    background: #FFF0F3;
    border-color: #FF2442;
    box-shadow: 0 4px 12px rgba(255, 36, 66, 0.2);
}

/* 激活状态（当当天是当前选中） */
.day-badge.active {
    background: #FF2442;
    color: #FFFFFF;
    border-color: #FF2442;
    box-shadow: 0 4px 12px rgba(255, 36, 66, 0.3);
}
```

#### 特殊说明
- Day标签使用**次按钮样式**
- 圆角统一为4px（更紧凑）
- 字重为700（加粗显示）
- 内边距为6px 16px（更紧凑）

#### 使用场景
- Day 1 标签
- Day 2 标签
- Day 3 标签
- Day 4 标签
- Day 5 标签

### 3.4 标签间距规范

| 标签类型 | 标签间距 | 与内容间距 |
|---------|---------|-----------|
| **水平标签组** | 8px | 16px |
| **垂直标签组** | 8px | 16px |
| **标签与图标** | 4px | - |

---

## 🧭 4. 导航规范

### 4.1 导航链接

#### 样式定义
```css
.nav-link {
    /* 文字样式 */
    color: #333333;
    font-size: 14px;
    font-weight: 500;
    text-decoration: none;
    padding: 8px 12px;
    border-radius: 4px;
    
    /* 交互 */
    cursor: pointer;
    transition: all 0.2s ease;
    position: relative;
}

/* 悬停状态 */
.nav-link:hover {
    color: #FF2442;
    background: #FFF0F3;
}

/* 激活状态 */
.nav-link.active {
    color: #FF2442;
    background: #FFF0F3;
    font-weight: 600;
}

/* 底部指示条（可选） */
.nav-link::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 50%;
    width: 0;
    height: 2px;
    background: #FF2442;
    transform: translateX(-50%);
    transition: width 0.2s ease;
}

.nav-link.active::after {
    width: 80%;
}
```

#### 尺寸规范

| 元素 | 高度 | 内边距 | 字体大小 | 用途 |
|------|------|--------|---------|------|
| **桌面导航** | 44px | 8px 16px | 14px | 顶部导航栏 |
| **移动导航** | 48px | 12px 16px | 16px | 底部导航栏 |

#### 使用场景
- 顶部导航栏链接
- 底部导航栏链接
- 面包屑导航

### 4.2 导航间距规范

| 元素 | 间距值 |
|------|--------|
| **桌面导航项间距** | 24px |
| **移动导航项间距** | 16px |
| **导航与内容间距** | 0（粘连布局） |

---

## 📐 5. 组件清单

### 5.1 Hero区域组件

| 组件名称 | 当前样式 | 目标样式 | 样式类型 | 优先级 |
|---------|---------|---------|---------|--------|
| **"开始探索"按钮** | 渐变按钮 | 小红书主按钮 | 主按钮 | P0 |
| **"2026年2月"标签** | 半透明标签 | 小红书次按钮 | 次按钮 | P0 |
| **地点标签** | 半透明标签 | 小红书次按钮 | 次按钮 | P1 |
| **主题标签组** | 渐变标签 | 小红书实心标签 | 实心标签 | P1 |

### 5.2 Day标签组件

| 组件名称 | 当前样式 | 目标样式 | 样式类型 | 优先级 |
|---------|---------|---------|---------|--------|
| **Day 1 标签** | 渐变标签 | 小红书次按钮 | 次按钮 | P0 |
| **Day 2 标签** | 渐变标签 | 小红书次按钮 | 次按钮 | P0 |
| **Day 3 标签** | 渐变标签 | 小红书次按钮 | 次按钮 | P0 |
| **Day 4 标签** | 渐变标签 | 小红书次按钮 | 次按钮 | P0 |
| **Day 5 标签** | 渐变标签 | 小红书次按钮 | 次按钮 | P0 |

**样式特征**：
- 背景：白色 `#FFFFFF`
- 文字：小红书红色 `#FF2442`
- 边框：1px solid `#FF2442`
- 圆角：4px
- 内边距：6px 16px
- 字重：700

### 5.3 景点标签组件

| 组件名称 | 当前样式 | 目标样式 | 样式类型 | 优先级 |
|---------|---------|---------|---------|--------|
| **"历史文化"标签** | 珊瑚色标签 | 小红书实心标签 | 实心标签 | P1 |
| **"自然风光"标签** | 珊瑚色标签 | 小红书实心标签 | 实心标签 | P1 |
| **"海滩度假"标签** | 珊瑚色标签 | 小红书实心标签 | 实心标签 | P1 |
| **"地质奇观"标签** | 珊瑚色标签 | 小红书实心标签 | 实心标签 | P1 |
| **"摄影圣地"标签** | 珊瑚色标签 | 小红书实心标签 | 实心标签 | P1 |
| **"自然奇观"标签** | 珊瑚色标签 | 小红书实心标签 | 实心标签 | P1 |

**样式特征**：
- 背景：小红书红色 `#FF2442`
- 文字：白色 `#FFFFFF`
- 圆角：6px
- 内边距：4px 12px
- 字重：600

### 5.4 美食标签组件

| 组件名称 | 当前样式 | 目标样式 | 样式类型 | 优先级 |
|---------|---------|---------|---------|--------|
| **"必尝"标签** | 渐变标签 | 小红书实心标签 | 实心标签 | P1 |
| **"特色"标签** | 渐变标签 | 小红书实心标签 | 实心标签 | P1 |

**样式特征**：
- 背景：小红书红色 `#FF2442`
- 文字：白色 `#FFFFFF`
- 圆角：6px
- 内边距：4px 12px
- 字重：600

### 5.5 主题标签组件

| 组件名称 | 当前样式 | 目标样式 | 样式类型 | 优先级 |
|---------|---------|---------|---------|--------|
| **"🏖️ 海滨度假"标签** | 渐变标签 | 小红书实心标签 | 实心标签 | P1 |
| **"🏛️ 历史文化"标签** | 渐变标签 | 小红书实心标签 | 实心标签 | P1 |
| **"🍽️ 美食探索"标签** | 渐变标签 | 小红书实心标签 | 实心标签 | P1 |
| **"🌊 水上活动"标签** | 渐变标签 | 小红书实心标签 | 实心标签 | P1 |
| **"📸 摄影打卡"标签** | 渐变标签 | 小红书实心标签 | 实心标签 | P1 |

**样式特征**：
- 背景：小红书红色 `#FF2442`
- 文字：白色 `#FFFFFF`
- 圆角：6px
- 内边距：4px 12px
- 字重：600
- 保留emoji图标

### 5.6 导航菜单组件

| 组件名称 | 当前样式 | 目标样式 | 样式类型 | 优先级 |
|---------|---------|---------|---------|--------|
| **导航链接（默认）** | 蓝色文字链接 | 小红书次按钮样式 | 次按钮 | P1 |
| **导航链接（激活）** | 蓝色文字+下划线 | 小红书红色文字 | 激活状态 | P1 |
| **导航链接（悬停）** | 蓝色文字+下划线 | 小红书红色文字+浅红背景 | 悬停状态 | P1 |

**样式特征**：
- 默认：文字色 `#333333`，无背景
- 悬停：文字色 `#FF2442`，背景 `#FFF0F3`
- 激活：文字色 `#FF2442`，背景 `#FFF0F3`，字重600

### 5.7 其他按钮组件

| 组件名称 | 当前样式 | 目标样式 | 样式类型 | 优先级 |
|---------|---------|---------|---------|--------|
| **返回顶部按钮** | 蓝色圆形按钮 | 小红书红色圆形按钮 | 主按钮 | P2 |
| **图片标签** | 黑色半透明 | 小红书红色半透明 | 实心标签 | P2 |
| **导航切换按钮** | 深灰色图标 | 小红书红色图标 | 次按钮 | P2 |

---

## 🎯 6. 交互状态规范

### 6.1 悬停效果（Hover）

| 组件类型 | 悬停效果 | 过渡时间 | 缓动函数 |
|---------|---------|---------|---------|
| **主按钮** | 背景色变深 + 上移1px + 阴影加深 | 0.2s | ease |
| **次按钮** | 背景变浅红 + 阴影加深 | 0.2s | ease |
| **实心标签** | 背景色变深 + 上移1px | 0.2s | ease |
| **空心标签** | 背景变浅红 | 0.2s | ease |
| **导航链接** | 文字变红 + 背景变浅红 | 0.2s | ease |

### 6.2 激活效果（Active）

| 组件类型 | 激活效果 | 过渡时间 | 缓动函数 |
|---------|---------|---------|---------|
| **次按钮** | 背景变红 + 文字变白 | 0.2s | ease |
| **导航链接** | 文字变红 + 背景变浅红 + 字重加粗 | 0.2s | ease |
| **空心标签** | 背景变红 + 文字变白 | 0.2s | ease |

### 6.3 点击效果（Pressed）

| 组件类型 | 点击效果 | 过渡时间 | 缓动函数 |
|---------|---------|---------|---------|
| **主按钮** | 背景色变深 + 回到原位 | 0.1s | ease-in |
| **次按钮** | 背景变深 + 回到原位 | 0.1s | ease-in |

### 6.4 禁用状态（Disabled）

| 组件类型 | 禁用效果 | 鼠标样式 |
|---------|---------|---------|
| **主按钮** | 背景变浅粉 + 文字变白 + 无阴影 | not-allowed |
| **次按钮** | 背景变灰 + 边框变灰 + 文字变灰 | not-allowed |

---

## 💻 7. CSS变量定义

### 7.1 颜色变量

```css
:root {
    /* 小红书红色系列 */
    --xhs-red: #FF2442;
    --xhs-red-dark: #E61E3A;
    --xhs-red-darker: #D01A33;
    --xhs-red-light: #FFF0F3;
    --xhs-red-lighter: #FFE5EB;
    --xhs-red-faint: #FFB6C1;
    
    /* 中性色 */
    --xhs-white: #FFFFFF;
    --xhs-off-white: #FAFAFA;
    --xhs-dark-gray: #333333;
    --xhs-medium-gray: #666666;
    --xhs-light-gray: #999999;
    --xhs-border-gray: #E0E0E0;
    
    /* 渐变 */
    --xhs-gradient: linear-gradient(135deg, #FF2442 0%, #FF6B9D 100%);
    --xhs-gradient-hover: linear-gradient(135deg, #E61E3A 0%, #FF5A8A 100%);
}
```

### 7.2 阴影变量

```css
:root {
    /* 小红书风格阴影 */
    --xhs-shadow-sm: 0 2px 8px rgba(255, 36, 66, 0.15);
    --xhs-shadow-md: 0 4px 16px rgba(255, 36, 66, 0.25);
    --xhs-shadow-lg: 0 8px 24px rgba(255, 36, 66, 0.3);
    --xhs-shadow-hover: 0 12px 32px rgba(255, 36, 66, 0.4);
    
    /* 通用阴影 */
    --xhs-shadow-none: none;
}
```

### 7.3 圆角变量

```css
:root {
    --xhs-radius-sm: 4px;   /* 小圆角 - Day标签 */
    --xhs-radius-md: 6px;   /* 中圆角 - 标签 */
    --xhs-radius-lg: 8px;   /* 大圆角 - 按钮 */
    --xhs-radius-xl: 12px;  /* 超大圆角 - 卡片 */
}
```

### 7.4 间距变量

```css
:root {
    /* 按钮内边距 */
    --xhs-btn-padding-lg: 16px 32px;
    --xhs-btn-padding-md: 12px 24px;
    --xhs-btn-padding-sm: 8px 16px;
    
    /* 标签内边距 */
    --xhs-tag-padding-lg: 6px 14px;
    --xhs-tag-padding-md: 4px 12px;
    --xhs-tag-padding-sm: 3px 10px;
    
    /* Day标签内边距 */
    --xhs-day-padding: 6px 16px;
}
```

### 7.5 过渡变量

```css
:root {
    --xhs-transition-fast: 0.1s ease;
    --xhs-transition-normal: 0.2s ease;
    --xhs-transition-slow: 0.3s ease;
}
```

### 7.6 字体变量

```css
:root {
    /* 字体大小 */
    --xhs-font-size-xs: 10px;
    --xhs-font-size-sm: 12px;
    --xhs-font-size-base: 14px;
    --xhs-font-size-lg: 16px;
    --xhs-font-size-xl: 18px;
    
    /* 字重 */
    --xhs-font-weight-normal: 400;
    --xhs-font-weight-medium: 500;
    --xhs-font-weight-semibold: 600;
    --xhs-font-weight-bold: 700;
}
```

---

## 📱 8. 响应式设计

### 8.1 按钮响应式

| 屏幕尺寸 | 主按钮高度 | 主按钮内边距 | 次按钮高度 | 次按钮内边距 |
|---------|-----------|-------------|-----------|-------------|
| **桌面端 (≥1200px)** | 44px | 12px 24px | 36px | 8px 20px |
| **平板端 (768-1199px)** | 40px | 10px 20px | 34px | 7px 18px |
| **移动端 (576-767px)** | 44px | 12px 24px | 36px | 8px 20px |
| **小屏端 (<576px)** | 44px | 12px 24px | 36px | 8px 20px |

### 8.2 标签响应式

| 屏幕尺寸 | 实心标签高度 | 实心标签内边距 | Day标签高度 | Day标签内边距 |
|---------|-------------|---------------|-----------|-------------|
| **桌面端 (≥1200px)** | 24px | 4px 12px | 32px | 6px 16px |
| **平板端 (768-1199px)** | 24px | 4px 12px | 32px | 6px 16px |
| **移动端 (576-767px)** | 24px | 4px 12px | 32px | 6px 16px |
| **小屏端 (<576px)** | 24px | 4px 12px | 32px | 6px 16px |

### 8.3 导航响应式

| 屏幕尺寸 | 导航链接高度 | 导航链接内边距 | 导航项间距 |
|---------|-------------|---------------|-----------|
| **桌面端 (≥1200px)** | 44px | 8px 16px | 24px |
| **平板端 (768-1199px)** | 44px | 8px 12px | 16px |
| **移动端 (576-767px)** | 48px | 12px 16px | 0（汉堡菜单） |
| **小屏端 (<576px)** | 48px | 12px 16px | 0（汉堡菜单） |

---

## ♿ 9. 可访问性

### 9.1 颜色对比度

| 元素 | 前景色 | 背景色 | 对比度 | WCAG标准 | 状态 |
|------|--------|--------|--------|---------|------|
| **主按钮文字** | #FFFFFF | #FF2442 | 4.5:1 | AA | ✅ 通过 |
| **主按钮悬停文字** | #FFFFFF | #E61E3A | 4.8:1 | AA | ✅ 通过 |
| **次按钮文字** | #FF2442 | #FFFFFF | 4.5:1 | AA | ✅ 通过 |
| **次按钮悬停文字** | #FF2442 | #FFF0F3 | 4.2:1 | AA | ✅ 通过 |
| **实心标签文字** | #FFFFFF | #FF2442 | 4.5:1 | AA | ✅ 通过 |
| **空心标签文字** | #FF2442 | #FFFFFF | 4.5:1 | AA | ✅ 通过 |
| **导航链接文字** | #333333 | #FFFFFF | 12.6:1 | AAA | ✅ 通过 |
| **导航链接激活文字** | #FF2442 | #FFF0F3 | 4.2:1 | AA | ✅ 通过 |

### 9.2 触摸目标尺寸

| 元素类型 | 最小尺寸 | 推荐尺寸 | 状态 |
|---------|---------|---------|------|
| **主按钮** | 44px × 44px | 44px × 120px | ✅ 符合 |
| **次按钮** | 36px × 36px | 36px × 80px | ✅ 符合 |
| **Day标签** | 32px × 32px | 32px × 自适应 | ✅ 符合 |
| **实心标签** | 24px × 24px | 24px × 自适应 | ⚠️ 较小 |
| **导航链接** | 44px × 44px | 44px × 自适应 | ✅ 符合 |

**注意**：标签尺寸较小，但作为非主要操作元素可以接受。如果需要增强可点击性，可以增加透明点击区域。

### 9.3 键盘导航

```css
/* 焦点样式 */
.btn-primary:focus,
.btn-secondary:focus,
.nav-link:focus,
.day-badge:focus {
    outline: 2px solid #FF2442;
    outline-offset: 2px;
}

/* 标签焦点样式 */
.tag-solid:focus,
.tag-outline:focus {
    outline: 2px solid #FF2442;
    outline-offset: 2px;
}

/* 跳过链接（可选） */
.skip-link {
    position: absolute;
    top: -40px;
    left: 0;
    background: #FF2442;
    color: #FFFFFF;
    padding: 8px 16px;
    z-index: 100;
    transition: top 0.3s;
}

.skip-link:focus {
    top: 0;
}
```

### 9.4 屏幕阅读器支持

```html
<!-- 按钮示例 -->
<button class="btn-primary" aria-label="开始探索行程">
    <i class="fas fa-arrow-down" aria-hidden="true"></i>
    开始探索
</button>

<!-- Day标签示例 -->
<span class="day-badge" role="status" aria-label="第一天行程">
    Day 1
</span>

<!-- 标签示例 -->
<span class="tag-solid" aria-label="必尝美食">
    <i class="fas fa-star" aria-hidden="true"></i>
    必尝
</span>
```

---

## 🚀 10. 实施建议

### 10.1 CSS修改建议

#### 1. 修改CSS变量（优先级：P0）
```css
/* 在 :root 中添加小红书红色变量 */
:root {
    /* 保留原有变量 */
    --ocean-blue: #4A90E2;
    --deep-ocean: #2C5F8D;
    
    /* 新增小红书红色变量 */
    --xhs-red: #FF2442;
    --xhs-red-dark: #E61E3A;
    --xhs-red-light: #FFF0F3;
    
    /* 修改渐变变量 */
    --hero-gradient: linear-gradient(135deg, #FF2442 0%, #FF6B9D 50%, #FF2442 100%);
    --button-gradient: linear-gradient(135deg, #FF2442 0%, #FF6B9D 100%);
    --tag-gradient: linear-gradient(90deg, #FF2442 0%, #FF6B9D 100%);
}
```

#### 2. 修改Hero按钮（优先级：P0）
```css
.hero-btn {
    background: var(--xhs-red);
    color: var(--xhs-white);
    border: none;
    border-radius: 8px;
    box-shadow: 0 2px 8px rgba(255, 36, 66, 0.3);
}

.hero-btn:hover {
    background: var(--xhs-red-dark);
    box-shadow: 0 4px 12px rgba(255, 36, 66, 0.4);
    transform: translateY(-1px);
}
```

#### 3. 修改Hero标签（优先级：P0）
```css
.hero-tags .tag {
    background: var(--xhs-white);
    color: var(--xhs-red);
    border: 1px solid var(--xhs-red);
    border-radius: 8px;
    padding: 8px 20px;
    box-shadow: 0 2px 6px rgba(0, 0, 0, 0.08);
}

.hero-tags .tag:hover {
    background: var(--xhs-red-light);
}
```

#### 4. 修改Day标签（优先级：P0）
```css
.day-badge {
    background: var(--xhs-white);
    color: var(--xhs-red);
    border: 1px solid var(--xhs-red);
    border-radius: 4px;
    padding: 6px 16px;
    font-weight: 700;
    box-shadow: 0 2px 6px rgba(0, 0, 0, 0.08);
}

.day-badge:hover {
    background: var(--xhs-red-light);
    box-shadow: 0 4px 12px rgba(255, 36, 66, 0.2);
}
```

#### 5. 修改景点标签（优先级：P1）
```css
.attraction-tag {
    background: var(--xhs-red);
    color: var(--xhs-white);
    border: none;
    border-radius: 6px;
    padding: 4px 12px;
    font-weight: 600;
    box-shadow: 0 2px 4px rgba(255, 36, 66, 0.2);
}
```

#### 6. 修改美食标签（优先级：P1）
```css
.food-tag {
    background: var(--xhs-red);
    color: var(--xhs-white);
    border: none;
    border-radius: 6px;
    padding: 4px 12px;
    font-weight: 600;
    box-shadow: 0 2px 4px rgba(255, 36, 66, 0.2);
}
```

#### 7. 修改主题标签（优先级：P1）
```css
.theme-tag {
    background: var(--xhs-red);
    color: var(--xhs-white);
    border: none;
    border-radius: 6px;
    padding: 4px 12px;
    font-weight: 600;
    box-shadow: 0 2px 4px rgba(255, 36, 66, 0.2);
}
```

#### 8. 修改导航链接（优先级：P1）
```css
.nav-link {
    color: var(--xhs-dark-gray);
    padding: 8px 12px;
    border-radius: 4px;
}

.nav-link:hover {
    color: var(--xhs-red);
    background: var(--xhs-red-light);
}

.nav-link.active {
    color: var(--xhs-red);
    background: var(--xhs-red-light);
    font-weight: 600;
}
```

### 10.2 保持响应式设计

#### 关键原则
1. **不改变响应式断点**：保持现有的断点不变
2. **不改变布局结构**：只修改颜色、圆角、阴影等样式
3. **保持触摸目标尺寸**：确保移动端按钮尺寸符合标准
4. **保持字体大小**：不改变字体大小，只改变颜色和字重

#### 响应式检查清单
- [ ] 移动端（<576px）：按钮尺寸是否合适
- [ ] 平板端（768-1199px）：按钮和标签是否正常显示
- [ ] 桌面端（≥1200px）：导航链接是否正常显示
- [ ] 横屏模式：布局是否正常
- [ ] 深色模式（如有）：对比度是否足够

### 10.3 确保可访问性

#### 关键原则
1. **颜色对比度**：确保所有文字与背景对比度≥4.5:1（AA标准）
2. **触摸目标**：按钮最小尺寸44px×44px
3. **键盘导航**：所有交互元素可通过Tab键访问
4. **屏幕阅读器**：添加适当的ARIA标签

#### 可访问性检查清单
- [ ] 主按钮对比度≥4.5:1
- [ ] 次按钮对比度≥4.5:1
- [ ] 标签对比度≥4.5:1
- [ ] 导航链接对比度≥4.5:1
- [ ] 所有按钮有焦点样式
- [ ] 所有交互元素有适当的ARIA标签
- [ ] 图标有aria-hidden="true"或文字描述

### 10.4 渐进式实施

#### 阶段1：核心组件（P0优先级）
1. 修改CSS变量，添加小红书红色
2. 修改Hero区域"开始探索"按钮
3. 修改Hero区域"2026年2月"标签
4. 修改所有Day标签（Day 1-5）

#### 阶段2：重要组件（P1优先级）
1. 修改景点标签
2. 修改美食标签
3. 修改主题标签
4. 修改导航链接样式

#### 阶段3：增强组件（P2优先级）
1. 修改返回顶部按钮
2. 修改图片标签
3. 修改其他小型按钮
4. 优化悬停和点击效果

### 10.5 测试建议

#### 浏览器兼容性测试
- [ ] Chrome 90+
- [ ] Safari 14+
- [ ] Firefox 88+
- [ ] Edge 90+

#### 设备测试
- [ ] iPhone（iOS 14+）
- [ ] Android（Chrome最新版）
- [ ] iPad（iOS 14+）
- [ ] Android平板

#### 功能测试
- [ ] 按钮点击是否正常
- [ ] 悬停效果是否正常
- [ ] 响应式布局是否正常
- [ ] 滚动动画是否正常
- [ ] 键盘导航是否正常

#### 视觉测试
- [ ] 颜色是否准确
- [ ] 阴影是否自然
- [ ] 圆角是否统一
- [ ] 间距是否一致
- [ ] 对比度是否足够

---

## 📊 11. 实施优先级矩阵

| 组件 | 优先级 | 复杂度 | 影响范围 | 预计时间 |
|------|--------|--------|---------|---------|
| **Hero按钮** | P0 | 低 | Hero区域 | 5分钟 |
| **Hero标签** | P0 | 低 | Hero区域 | 5分钟 |
| **Day标签（5个）** | P0 | 低 | 5个Day区域 | 5分钟 |
| **景点标签（6个）** | P1 | 低 | 景点区域 | 5分钟 |
| **美食标签（2个）** | P1 | 低 | 美食区域 | 5分钟 |
| **主题标签（5个）** | P1 | 低 | 概览区域 | 5分钟 |
| **导航链接** | P1 | 中 | 全局导航 | 10分钟 |
| **返回顶部按钮** | P2 | 低 | 全局 | 5分钟 |
| **图片标签** | P2 | 低 | 多个区域 | 10分钟 |

**总预计时间**：约55分钟

---

## ✅ 12. 验收标准

### 12.1 视觉标准
- [ ] 所有按钮使用小红书红色 `#FF2442` 作为主色
- [ ] 所有标签使用小红书红色 `#FF2442` 作为背景或文字色
- [ ] 所有组件圆角符合规范（按钮8px，标签6px，Day标签4px）
- [ ] 所有阴影符合小红书风格
- [ ] 所有悬停效果平滑自然

### 12.2 功能标准
- [ ] 所有按钮可点击且正常工作
- [ ] 所有标签正确显示内容
- [ ] 所有导航链接正常跳转
- [ ] 响应式布局正常
- [ ] 键盘导航正常

### 12.3 可访问性标准
- [ ] 所有颜色对比度≥4.5:1（AA标准）
- [ ] 所有触摸目标≥44px×44px
- [ ] 所有交互元素可通过键盘访问
- [ ] 所有图标有适当的ARIA标签

### 12.4 兼容性标准
- [ ] 在主流浏览器中正常显示
- [ ] 在移动设备上正常显示
- [ ] 在不同屏幕尺寸下正常显示

---

## 📝 13. 注意事项

### 13.1 绝对不能变更的内容
⚠️ **重要提醒**：以下内容绝对不能变更

1. **HTML结构**：不能添加、删除或修改HTML元素
2. **页面内容**：不能修改任何文字内容、图片、链接
3. **布局结构**：不能改变页面的整体布局和排列方式
4. **JavaScript逻辑**：不能修改任何JavaScript代码
5. **响应式断点**：不能改变响应式断点
6. **字体大小**：不能改变字体大小（只能改变字重）
7. **间距尺寸**：不能改变主要的间距尺寸（只能微调）

### 13.2 只能修改的内容
✅ **允许修改的内容**

1. **颜色**：可以修改所有颜色属性
2. **圆角**：可以修改圆角大小
3. **阴影**：可以修改阴影样式
4. **边框**：可以修改边框样式
5. **字重**：可以修改字重（400/500/600/700）
6. **背景**：可以修改背景色和背景渐变
7. **过渡效果**：可以修改过渡动画
8. **悬停效果**：可以修改悬停状态的样式

### 13.3 需要特别关注的组件

#### Day标签
- **重要性**：P0，最高优先级
- **当前样式**：渐变背景
- **目标样式**：白色背景 + 红色边框 + 红色文字
- **特殊要求**：圆角4px（比其他标签更小）

#### 导航链接
- **重要性**：P1，高优先级
- **当前样式**：蓝色文字
- **目标样式**：默认深灰，悬停/激活红色
- **特殊要求**：需要保持现有的底部指示线样式（可选）

#### 景点/美食标签
- **重要性**：P1，高优先级
- **当前样式**：珊瑚色背景
- **目标样式**：小红书红色背景
- **特殊要求**：确保与卡片背景的对比度足够

---

## 📚 14. 参考资源

### 14.1 小红书设计规范
- [小红书设计系统](https://design.xiaohongshu.com/)（官方）
- 小红书品牌色：#FF2442
- 小红书字体：PingFang SC
- 小红书圆角风格：4px-8px

### 14.2 WCAG可访问性标准
- [WCAG 2.1 AA标准](https://www.w3.org/WAI/WCAG21/quickref/)
- 颜色对比度：4.5:1（AA）
- 触摸目标：44px×44px（最小）
- 键盘导航：所有交互元素可访问

### 14.3 现有设计文档
- `/Users/bukaixin/workspace/tours/2026年2月广西行/design-spec.md`
- `/Users/bukaixin/workspace/tours/2026年2月广西行/css/style.css`

---

## 🎉 15. 总结

本设计规范详细定义了小红书应用风格的UI组件，包括：

1. **色彩系统**：小红书红色系列，对比度符合WCAG AA标准
2. **按钮规范**：主按钮和次按钮，完整的状态定义
3. **标签规范**：实心标签、空心标签、Day标签，明确的尺寸规范
4. **交互状态**：悬停、激活、点击、禁用，平滑的过渡效果
5. **CSS变量**：完整的变量定义，便于统一管理
6. **组件清单**：所有需要修改的组件，明确的优先级
7. **实施建议**：详细的修改步骤，渐进式实施方案
8. **可访问性**：符合WCAG 2.1 AA标准

**核心原则**：绝对不能变更任何内容，只修改样式和布局。

---

**文档版本**：v1.0  
**创建日期**：2026年1月28日  
**最后更新**：2026年1月28日  
**设计风格**：小红书应用风格  
**适用项目**：2026年2月广西行旅行计划页面